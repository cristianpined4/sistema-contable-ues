import { renderDataDashdboard } from "../events/init.js";

const Dashboard = [
    {
        action: "DOMContentLoaded",
        event: renderDataDashdboard,
        element: document,
    }
]

export default Dashboard;